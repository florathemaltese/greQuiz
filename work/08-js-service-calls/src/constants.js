// A JavaScript module that exports an object called constants.
// The object has a property called page,  with two sub-properties: home and login.
// Defines the names of two pages in the project: home and login.
const constants = {
    page: {
      home: "home",
      login: "login",
    },
  };
// Exports the constants object, making it available for other modules to import and use.
  module.exports = constants;